# Shell scripts run inside Linux containers and on Mac/Linux hosts. A Windows
# checkout with core.autocrlf would give them CRLF endings, and /bin/sh then
# fails with "not found". Keep them LF everywhere.
*.sh text eol=lf
